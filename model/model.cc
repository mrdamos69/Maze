#include "model.h"

// int s21::Model::is_digit(char expression) {
//   int error = 1;
//   if ((expression >= '0' && expression <= '9') || expression == '.') {
//     error = 0;
//   }
//   return error;
// }
